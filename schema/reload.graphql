type Query {
  reloadModelRemotely(id: ID!, parentId: ID, serverUuid: String!, entity: Entity!, del:Boolean): Boolean
}

enum Entity {
  ACTION,
  ATTRIBUTE,
  ATTRIBUTE_GROUP,
  CHANNEL,
  DASHBOARD,
  IMPORT_CONFIG,
  LANGUAGE,
  RELATION,
  ROLE,
  TYPE,
  USER
}